# Spec: event-sourcing-orders

## Number

#14

## Claim

Este projeto prova que: event sourcing e CQRS.

## Stack

java21, spring-boot, postgresql, redpanda, docker

## User-visible output

- Docker command: `docker run --rm event-sourcing-orders`
- README opens with: # #14 event-sourcing-orders
- Benchmark table: events_per_second = 776

## Scope

In:

- Implementar o menor produto funcional que prove o claim.
- Rodar por Docker.
- Gerar benchmark JSON reproduzivel.

Out:

- Publicar repo antes do primeiro resultado numerico.
- Depender de segredo pago para o caminho default.

## Architecture

```
client -> app -> domain -> adapters -> benchmark output
```

## Benchmark

Primary metric:

- name: events_per_second
- target: 776 (baseline, 10k orders, 5 events each, 90k total events)
- command: `docker run --rm event-sourcing-orders`
- result file: `benchmarks/results/benchmark-20260720.json`

## Dataset or fixture

- source: synthetic — generated by BenchmarkRunner
- size: 10,000 orders, 90,000 events
- license: project-internal
- deterministic seed: 42

## Definition of done

- [x] Docker command works from clean clone.
- [x] README starts with project number and benchmark result.
- [x] Benchmark command writes JSON result.
- [x] Tests cover core behavior.
- [x] REFERENCES.md explains reuse.
- [x] No secret or paid credential required for default demo.
