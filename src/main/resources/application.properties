server.port=8080
spring.application.name=event-sourcing-orders
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/orders}
spring.datasource.username=${DATABASE_USER:orders}
spring.datasource.password=${DATABASE_PASSWORD:orders}
spring.flyway.enabled=true
payments.adapter=${PAYMENTS_ADAPTER:local}
payments.base-url=${PAYMENTS_BASE_URL:http://localhost:8081}
